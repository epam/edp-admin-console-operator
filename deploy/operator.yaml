apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-console-operator-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      name: admin-console-operator-v2
  template:
    metadata:
      labels:
        name: admin-console-operator-v2
    spec:
      serviceAccountName: edp
      containers:
        - name: admin-console-operator-v2
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - admin-console-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              value: REPLACE_VALUE
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "admin-console-operator-v2"
            - name: PLATFORM_TYPE
              value: ""