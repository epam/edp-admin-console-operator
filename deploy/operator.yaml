apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-console-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: admin-console-operator
  template:
    metadata:
      labels:
        name: admin-console-operator
    spec:
      serviceAccountName: admin-console-operator
      containers:
        - name: admin-console-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - admin-console-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "admin-console-operator"
